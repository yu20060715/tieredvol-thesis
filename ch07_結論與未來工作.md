# 第七章 結論與未來工作

> **本章導讀**：7.1 回顧核心宣稱與教訓性結論；7.2 貢獻總結；7.3 未來工作；
> 7.4 正面回應「下一個最大的挑戰是什麼」的開放問題。

## 7.1 結論

本論文設計、實作並評估了 **TieredVol**：一個以加權條帶化為核心的 Linux 核心異質
磁碟儲存系統（Device Mapper target）。針對「多顆性能差異巨大的磁碟並存」的現實
情境，TieredVol 以**權重化分配**取代等權條帶，以**確定性 O(1) 映射**保證正確性，
以**瓶頸模型與 auto_weight** 引導權重選擇，並以**權重借調、鏡像、重建、壞區、
寫入快取**在單一 target 內完成動態平衡與容錯。

實驗證實的核心宣稱（各項證據見第六章）：

1. **加權條帶正確且確定**：分布逐 byte 精確（多組權重計數器全對，6.2.1）、
   映射 O(1) 純算術無表無鎖、全程 `err=0`、8 G crc32c verify 0 mismatch（6.2.2）。
2. **瓶頸模型成立**：`min(soloᵢ × ΣW/wᵢ)` 在 1–4 碟、多組權重、排水態與冷態下
   命中 ±5% 內（6.3.1）；最純展示為排水態疊碟（6:1:1:1）的 `D×9 ≈ 1760` 與
   B@x4 四碟 [64:27:9:4] 的 `solo_A×104/64 ≈ 2517`（0% 偏差）兩次精確命中。
3. **自動權重達近最優**：`auto_weight` 窮舉權重使寫入階梯嚴格單調
   （2078 < 2410 < 2896 < 3006）、Σsolo 利用率 ≥ 99.6%，S4 較等權重提升 52%（6.3.2）。
4. **共享匯流排需 DMI-aware 權重**：比例權重在三碟共用 PCH DMI 時掉到 2561 MB/s，
   DMI-aware 權重恢復至 3370 MB/s（+32%），並以 `solo_A + DMI` 數學上解釋了
    「三碟寫入不會超過兩碟」的硬體天花板；四碟 [64:27:9:4] 下 DMI 預算把瓶頸
   交還 A 碟、實測 2517 以 0% 偏差命中（6.3.1、6.3.4）。
5. **可靠性機制整合**：鏡像、重建、壞區、權重借調（含持久化與 reload 恢復）、
   小寫入快取在單一 target 內運作，無引入額外錯誤（6.2、6.5）。

**教訓性結論**：異質碟條帶化的關鍵不在「權重正確」（那是確定性問題——任何權重
都能做到分布正確），而在**權重與實際速比的匹配**（那是性能問題）。權重失配即使
分布精確也會損失 36% 以上吞吐（6.3.3）；而「速比」本身又受 PCIe 插槽（3.7 倍，
6.3.5）與共享匯流排 DMI（封頂於 `solo_A + DMI`，6.3.4）影響——**效能工程必須在
真實拓撲上量測，否則模型與數據都會失真**。

## 7.2 貢獻總結

1. **加權條帶化 + 確定性無表 O(1) 映射**：純四則運算、逐 byte 精確、可重現、
   熱路徑無鎖（第三章 3.2）。
2. **瓶頸性能模型與 DMI-aware 權重分配**：`min(solo×ΣW/w)` 之證明與跨場景驗證；
   共享匯流排下的權重修正公式（3.3）。
3. **auto_weight 窮舉式自動權重工具**：由量測自動求近最優權重（近最優、+52%）。
4. **動態權重借調**：靜態佈局上的例外機制，持久化/reload 恢復（第四章 4.1）。
5. **多機制單 target 整合**：鏡像、重建、壞區、併發一致性、寫入快取（4.2–4.5）。
6. **四層測試架構與真實平台剖析**：單元→核心模擬→dmsetup→fio；揭露插槽、
   DMI、SLC 三大硬體洞見（第五章 5.8、第六章 6.3）。

## 7.3 未來工作

1. **平台拓展（無 DMI 牆的多碟擴展）**：B650M AM5（全 PCIe3.0/4.0 x4 直連 CPU）
   上驗證 (3.2) 在「各碟皆直連」下可達 `Σsolo`；預期推翻「三碟寫入 ≤ 兩碟」的
   B85 特例，證明模型的一般性。
2. **斷電一致性**：為 WC 增加日誌/寫保護或 CRC-striped 重放，提供 crash guarantee；
   加入 `FLUSH/FUA` 嚴格語義測試與 power-loss fault injection（`pm_test`/QEMU）。
3. **動態權重（線上低頻重調）**：在確定性佈局下，依**線上量測的穩定 solo** 低頻
   重算權重並調整後續 stripe 分配（不破壞既有位址），補齊「硬體老化/衰退」情境
   （實驗中 sdb solo 衰退 346→216 曾使 S4 瓶頸從 A 轉移至 D，
   6:1:1:1 下 S4 卡在 D×9≈1976、實測 1762，6.3.1/6.3.3）。
4. **規模與多佇列**：>4 碟、多控制器、NVMe-oF 網狀拓撲；多佇列/NUMA 感知的平行
   拆分與提交。
5. **QoS 與策略**：per-權重類別的優先權/限速（與權重正交的流量整形），滿足
   多租戶與即時性需求。
6. **開源與可重現性**：將量測協定（P1/P2/P3）與工具鏈整理為一鍵重現的
   benchmark suite，附環境探勘腳本（by-id 偵測、插槽拓撲報告），供社群複現
   本論文全部結果。

> 未來工作 1 與 6 有直接相依：平台拓展的結果需要可重現的測量工具支撐，
> 建議先完成 6 的套件整理，再做 1 的跨平台驗證。

## 7.4 開放問題

除上述可規劃的未來工作外，本研究暴露了幾個值得後續研究的**開放問題**：

1. **不重建映射的權重重調**：目前權重調整需重建卷（或僅影響後續 stripe）。
   能否在不移動既有資料的前提下，用數學方法「重新平衡」各碟既有資料的分布？
   ——這涉及 3.2 映射的可逆性與遷移成本的最小化。
2. **借調的容量規劃**：實驗顯示借調效益與拓撲相關——D 真為瓶頸時 +3%
   （6:1:1:1）、定案權重下中性（D 份額 3.85% 非瓶頸，B4）——何時值得借、
   借用區要保留多少 over-provisioning 才是合理的？極端退化（慢碟持續長時間
   變慢）下，借調是否會飽和借用區、退化成非確定性？實作上另受 kmalloc 限制
   （20G carve 約 2.6 MB、100G carve 超限 ENOMEM，6.5）——需要借調的
   隨機/排隊模型。
3. **多佇列/NUMA 下的順序語義**：平行拆分在多佇列與 NUMA 平台上是否仍保持
   per-位址的嚴格順序？跨 NUMA 節點的 sub-bio 提交是否需要新的完成語義？
4. **WC 斷電保護的成本效益**：日誌/寫保護的效能成本與一致性收益之間的
   取捨點在哪？能否做到「寫入保護常開、快取帶命中不降」？
5. **模型在無 DMI 牆平台的一般性**：(3.2) 是「各碟自覺瓶頸」，(3.3) 是「共享
   資源瓶頸」；全直連平台上是否存在第三種瓶頸（如控制器、PCIe root complex）？

> 開放問題的意義：界定「本論文做到哪裡、後人能從哪裡接手」，也是口試時
> 「下一步最大挑戰」這題的正面回答。
